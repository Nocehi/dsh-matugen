{
  "name": "dsh-matugen",
  "version": "0.1.0",
  "description": "Matugen/DankMaterialShell palette bridge for DeepSeek Harness Web themes",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./lib/client.js",
    "./core": "./src/core.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "src",
    "examples",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsdown --config tsdown.config.mjs",
    "prepare": "npm run build",
    "test": "node --test tests/*.test.js",
    "test:artifact": "node --test tests/client-bundle.test.js",
    "check": "node --check src/core.js && node --check src/index.js && node --check src/client.js && npm run build && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nocehi/dsh-matugen.git"
  },
  "dsh": {
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-ui-theme"
      ],
      "platform": "web",
      "immediately": true
    }
  },
  "devDependencies": {
    "tsdown": "^0.22.2"
  }
}
